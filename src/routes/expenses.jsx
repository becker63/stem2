import AppBar from "../components/AppBar";

export default function expenses() {
    return (
      <main style={{ padding: "1rem 0" }}>
        <AppBar />
        <h2>expenses</h2>
      </main>
    );
  }